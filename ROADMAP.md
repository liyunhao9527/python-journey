# Python 学习路线

面向 TypeScript / Node.js 开发者，从熟悉代码的行为出发理解 Python。路线是教学安排，不是已掌握清单。7 个阶段、29 节规划课程；目前只开放第一课。按过关证据推进，不预设周数。

| 阶段         | 课程  | 内容                                                           | 过关证据                                   |
| ------------ | ----- | -------------------------------------------------------------- | ------------------------------------------ |
| 1 基础语法   | 01—07 | uv 与解释器、变量与对象、字符串、容器、控制流、函数、推导式    | 独立写账单计算，解释类型转换和共享引用     |
| 2 日常开发   | 08—12 | 模块、文件、异常、环境与依赖、测试调试                         | 读取 CSV，报告错误行，拆模块并测试         |
| 3 项目一     | 13—14 | 文件清单 CLI                                                   | 真实目录扫描、JSON、退出码、边界测试       |
| 4 进阶语言   | 15—20 | 类与 dataclass、对象协议、类型提示、迭代器、装饰器、上下文管理 | 惰性处理数据，正确释放资源，说明抽象的用途 |
| 5 项目二     | 21—22 | 支出分析报告                                                   | 金额对账、幂等导入、SQLite、图表与离线报告 |
| 6 网络与并发 | 23—26 | HTTP、事务、asyncio、线程与进程                                | 超时与取消可测试，按工作负载选择并发方案   |
| 7 项目三     | 27—29 | 学习书签 API                                                   | CRUD、校验、迁移、接口测试、部署说明       |

## 从 TypeScript 迁移的安排

| 阶段         | 已有经验                                   | Python 学习重点与对照边界                                                            |
| ------------ | ------------------------------------------ | ------------------------------------------------------------------------------------ |
| 1 基础语法   | let / const、模板字符串、Array / Map、函数 | 名字绑定、f-string、list / dict、参数与推导式；验证可变对象、相等和身份              |
| 2 日常开发   | import、Node fs、npm、测试框架             | import、pathlib、uv、pytest；区分解释器环境、依赖安装与项目元数据                    |
| 3 项目一     | 曾经写过 Node CLI                          | 独立实现 Python 文件清单工具；不提供整套 TS 平行实现                                 |
| 4 进阶语言   | interface、类、迭代协议、生成器            | 类型提示、dataclass、Protocol、生成器、装饰器和 with；类比只解释共同目标             |
| 5 项目二     | 结构化数据与脚本处理                       | 以 Python 的 CSV、Decimal、SQLite 和图表生态交付报告                                 |
| 6 网络与并发 | Promise、async / await、Node 事件循环      | 协程对象与任务调度、阻塞 I/O、线程和进程；不把调用 async def 等同于启动 Promise 工作 |
| 7 项目三     | 调用和设计 REST API                        | Python 实现服务与持久化，TS 只用于客户端调用和验收                                   |

基础课采用“同一个任务 → 两种语言实现 → 差异解释 → 自己改写 Python”的顺序。按练习证据决定下一课，不按日历推进，也不重复从零讲解已熟悉的通用编程概念。遇到 Python 特有规则时单独放慢。

## 容易误判的概念

- Python 变量是对象的名字，没有 TS 的 `const` 绑定声明；也不能把 Python 的 `==` 当作 JS 对象的引用相等判断。
- `is` 判断身份，`==` 判断相等；`None` 通常用 `is None`。
- 类型提示不自动执行运行时校验；边界输入仍须验证。
- 生成器通常只能迭代一次；先理解这一点，再用它处理大文件。
- 协程并发不等于 CPU 并行。常规带 GIL 的 CPython 中，多线程不一定加速纯 Python CPU 计算；free-threaded 构建是另一个需要单独核实的运行条件。
- 先用标准库做小任务，再按需求引入第三方库，不把学习变成框架安装清单。

## 项目推进

详见 [项目任务书](projects/index.html)。每个项目交付 README、可复现输入、运行命令、边界测试与结果证据。项目启动时核实第三方 API 与依赖版本，再记录锁定方式。

完成主线后，按目标选一个方向：自动化与打包、数据分析与统计、Web 后端，或机器学习基础。先选择一个方向继续做项目。

## 项目工具约定

使用 uv 管理 Python 版本与依赖；第一课覆盖 `uv python install`、`uv python pin`、`uv sync` 和 `uv run`。第 11 课再展开 `uv add`、依赖分组和锁文件更新。课程命令统一从项目根目录执行，不使用特定系统的虚拟环境路径。
