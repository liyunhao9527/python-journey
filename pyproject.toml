[project]
name = "python-journey"
version = "0.1.0"
description = "Python learning notes, exercises and project briefs"
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py312"
